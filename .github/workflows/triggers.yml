name: HolaMundoConBash
on: 
  push:
    branches:
      - aprendiendo-github-actions
      - main
  workflow_dispatch:
  schedule:
    - cron: "30 2 * * *"
jobs:
  saluda-al-mundo:
    runs-on: ubuntu-latest
    steps:
      - name: Shows hello world
        run: echo "hola mundo"
